Create messages.po file:
xgettext -L Tcl -k_ ./*.tcl

add new file to messages.po:
xgettext -L Tcl -j -k_ ./new_file

convert messages.po to lang.msg (for example "ru"):
msgfmt --tcl -l ru -d ./   messages.po

all files in examples are in one dir 

